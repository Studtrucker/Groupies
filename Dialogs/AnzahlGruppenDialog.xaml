<Window x:Class="AnzahlGruppenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Skischule"
        mc:Ignorable="d"
        Height="200" Width="300">
    <DockPanel>
        <!--Header-->
        <DockPanel DockPanel.Dock="Top">
            <Image Source="/Images/icons8-3-48.png" Height="32" Width="48" />
            <TextBlock Height="Auto"  Margin="10">
                <TextBlock.Text>Anzahl Skikursgruppen</TextBlock.Text>
            </TextBlock>
        </DockPanel>
        
        <!--Bottom-->
        <DockPanel DockPanel.Dock="Bottom">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="100" Content="OK" Margin="10" IsDefault="True" Click="HandleButtonOKClick"/>
                <Button Width="100" Content="Abbruch" Margin="10" IsCancel="True" Click="HandleButtonCancelClick"/>
            </StackPanel>
        </DockPanel>

        <!--Rest-->
        <DockPanel>
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Wieviele Gruppen gebildet werden sollen" 
                           Grid.Column="0" Grid.Row="0" />
                <TextBlock x:Name="Count" Text="{Binding ElementName=slider, Path=Value}"
                           Grid.Column="1" Grid.Row="0" HorizontalAlignment="Right"/>
                <Slider x:Name="slider" Minimum="1" Maximum="15" TickFrequency="1" IsSnapToTickEnabled="True"
                        Grid.Column="0" Grid.Row="4" Grid.ColumnSpan="2"/>
                <!--Value="{Binding Anzahl, ValidatesOnDataErrors=True,UpdateSourceTrigger=PropertyChanged}"/>-->
            </Grid>

        </DockPanel>
    </DockPanel>
</Window>
