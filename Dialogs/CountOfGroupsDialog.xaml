<Window x:Class="CountOfGroupsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight" >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Style="{StaticResource RandRowDefinition}"/>
            <RowDefinition Height="*"/>
            <RowDefinition Style="{StaticResource RandRowDefinition}"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Column="1" Grid.Row="1">
            <!--Header-->
            <DockPanel DockPanel.Dock="Top">
            <Image Source="/Images/icons8-3-48.png" Height="32" Width="48" Style="{StaticResource WindowImageCaption}"/>
            <TextBlock Height="Auto" Style="{StaticResource WindowCaption}">
                <TextBlock.Text>Anzahl Skikursgruppen</TextBlock.Text>
            </TextBlock>
        </DockPanel>
            
            <!--Bottom-->
            <DockPanel DockPanel.Dock="Bottom">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Width="100" Content="OK" IsDefault="True" Click="HandleButtonOKClick"/>
                    <Button Width="100" Content="Abbruch" IsCancel="True" Click="HandleButtonCancelClick"/>
                </StackPanel>
            </DockPanel>
            
            <!--Rest-->
            <DockPanel>
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Neuen Skiclub erstellt."  Grid.Column="0" Grid.Row="0"/>
                    <TextBlock Grid.Column="0" Grid.Row="1">
                        Wieviele Skikursgruppen
                        <LineBreak/>                      
                        sollen angelegt werden?
                    </TextBlock>
                    <TextBox x:Name="Count" Text="{Binding ElementName=slider, Path=Value}"
                             Grid.Column="1" Grid.Row="1" HorizontalAlignment="Center" BorderBrush="Transparent"/>
                    <Slider x:Name="slider" Minimum="1" Maximum="15" TickFrequency="1" IsSnapToTickEnabled="True"
                            Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2"/>
                </Grid>
                
            </DockPanel>
        </DockPanel>
    </Grid>
</Window>
