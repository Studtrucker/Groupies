<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ribbon="clr-namespace:System.Windows.Controls.Ribbon;assembly=System.Windows.Controls.Ribbon"
        xmlns:uc="clr-namespace:Skischule.UserControls"
        xmlns:local="clr-namespace:Skischule"
        xmlns:convert="clr-namespace:Skischule.Converters"
        xmlns:en="clr-namespace:Skischule.Entities"
        mc:Ignorable="d"
        Icon="Images/ski-48.png"
        Title="Skischule" Height="450" Width="800"
        Loaded="HandleMainWindowLoaded" Closed="HandleMainwindowClosed" Closing="HandleMainWindowClosing">

    <Window.Resources>
        <convert:InvertableBoolToVisibleConverter Inverted="False" x:Key="BooleanToVisibility"/>
        <convert:InvertableBoolToVisibleConverter Inverted="True" x:Key="BooleanToVisibilityInverted"/>

        <!-- Style für die MenuItems -->
        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Foreground" Value="Black"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="false">
                    <Setter Property="Foreground" Value="Gray"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style für das DataGrid -->
        <Style x:Key="friendDataGridStyle" TargetType="{x:Type DataGrid}">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="RowBackground" Value="Beige"/>
            <Setter Property="AlternatingRowBackground" Value="LightGreen"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- Style für eine DataGridRow -->
        <Style x:Key="friendDataGridRowStyle" TargetType="{x:Type DataGridRow}">

        </Style>

    </Window.Resources>

    <DockPanel>

        <Menu Background="white" DockPanel.Dock="Top">

            <MenuItem  Header="_Datei" Background="white" Foreground="Black">
                <MenuItem Header="_Neue Skischule" Command="ApplicationCommands.New">
                    <MenuItem.Icon>
                        <Image Source="Images/neueGruppe-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Skischule ö_ffnen" Command="ApplicationCommands.Open">
                    <MenuItem.Icon>
                        <Image Source="Images/GruppeOeffnen-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Skischule importieren..." Command="{x:Static local:SkischuleBefehle.ImportTeilnehmerliste}">
                    <MenuItem.Icon>
                        <Image Height="20" Source="Images/icons8-microsoft-excel-2019-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="Skischule _speichern" Command="ApplicationCommands.Save">
                    <MenuItem.Icon>
                        <Image Source="Images/speichern-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Skischule speichern _unter..." Command="ApplicationCommands.SaveAs"/>
                <Separator/>
                <MenuItem Header="Einteilungen _drucken..." Command="ApplicationCommands.Print">
                    <MenuItem.Icon>
                        <Image Height="20" Source="Images/drucken-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="Zuletzt verwendet" x:Name="mostrecentlyUsedMenuItem"/>
                <Separator/>
                <MenuItem Header="_Beenden" Command="ApplicationCommands.Close" />
            </MenuItem>
            <MenuItem  Header="B_earbeiten" Background="white" Foreground="Black">
                <MenuItem Header="Neuer Teilnehmer" Command="{x:Static local:SkischuleBefehle.NeuerTeilnehmer}"/>
                <MenuItem Header="Teilnehmer löschen" Command="{x:Static local:SkischuleBefehle.TeilnehmerLoeschen}"/>
                <Separator/>
                <MenuItem Header="Neue Gruppe" Command="{x:Static local:SkischuleBefehle.NeueGruppe}"/>
                <MenuItem Header="Gruppe löschen" Command="{x:Static local:SkischuleBefehle.GruppeLoeschen}"/>
                <Separator/>
                <MenuItem Header="Neuer Skilehrer" Command="{x:Static local:SkischuleBefehle.NeuerUebungsleiter}"/>
                <MenuItem Header="Skilehrer löschen" Command="{x:Static local:SkischuleBefehle.UebungsleiterLoeschen}"/>
                <Separator/>
                <MenuItem Header="Neues Level" Command="{x:Static local:SkischuleBefehle.NeuesLevel}"/>
                <MenuItem Header="Level löschen" Command="{x:Static local:SkischuleBefehle.LevelLoeschen}"/>
            </MenuItem>

            <MenuItem  Header="Extras" Background="white" Foreground="Black">
                <MenuItem Header="Eigene Level definieren" Command="ApplicationCommands.New"/>
                <MenuItem Header="Skilehrer zuordnen" Click="MenuItem_Click"/>
            </MenuItem>

        </Menu>

        <TabControl TabStripPlacement="Left" Margin="5" BorderBrush="Black" >


            <TabItem Header="Teilnehmer" x:Name="tabitemTeilnehmer"  GotFocus="tabitemTeilnehmer_GotFocus" Margin="-2,-2,0,-2">

                <TabItem.LayoutTransform>
                    <RotateTransform Angle="270"/>
                </TabItem.LayoutTransform>

                <DockPanel>
                    <!-- Dock="Right" - Button, um den "Teilnehmer Explorer" aufzuklappen -->
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Panel.ZIndex="1">
                        <Button Margin="10,0,0,0" x:Name="btnShowTeilnehmerExplorer" MouseEnter="HandleButtonExpMouseEnter">Liste</Button>
                        <DockPanel LastChildFill="False" Width="100"/>
                        <StackPanel.LayoutTransform>
                            <RotateTransform Angle="90"/>
                        </StackPanel.LayoutTransform>
                    </StackPanel>

                    <Grid IsSharedSizeScope="True" >

                        <Grid x:Name="layerTeilnehmerdetails" MouseEnter="HandleLayerdetailsMouseEnter" Background="White" Margin="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="181*"/>
                                <RowDefinition Height="215*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid x:Name="gridTeilnehmerdetails" Grid.RowSpan="2" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>

                                <!-- Detailangaben -->
                                <Grid Grid.Row="1" Margin="2" >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="250"/>
                                        <ColumnDefinition Width="22*"/>
                                        <ColumnDefinition Width="115*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label Grid.Column="0" Grid.Row="0" Content="Vorname" Margin="2" />
                                    <Label Grid.Column="0" Grid.Row="1" Content="Name" Margin="2" />
                                    <Label Grid.Column="0" Grid.Row="2" Content="Persönliches Level" Margin="2" />
                                    <Label Grid.Column="0" Grid.Row="3" Content="Skikurs"  Margin="2" />

                                    <!-- Binding an den Datacontext des Windows -->
                                    <TextBox  Grid.Column="1" Grid.Row="0" Text="{Binding Path=Vorname, UpdateSourceTrigger=PropertyChanged}" Margin="2"/>
                                    <TextBox  Grid.Column="1" Grid.Row="1" Text="{Binding Path=Name, UpdateSourceTrigger=PropertyChanged}" Margin="2"/>
                                    <TextBox  Grid.Column="1" Grid.Row="2" Text="{Binding Path=PersoenlichesLevel.Benennung, UpdateSourceTrigger=PropertyChanged}" Margin="2"/>
                                    <TextBox  Grid.Column="1" Grid.Row="3" Text="{Binding Path=Skikurs, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Margin="2,2,2,2"/>

                                </Grid>
                            </Grid>
                        </Grid>

                        <Grid x:Name="layerTeilnehmerliste" Visibility="Collapsed" Margin="2"  >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition SharedSizeGroup="pinTeilnehmerSpalte" Width="330"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RenderTransform>
                                <TranslateTransform x:Name="layerTeilnehmerlisteTrans"/>
                            </Grid.RenderTransform>

                            <Border x:Name="borderTeilnehmerliste" BorderThickness="1" BorderBrush="Black" Grid.Column="1">
                                <DockPanel>
                                    <Grid x:Name="teilnehmerGrid" DockPanel.Dock="Top" >


                                        <TextBlock Text="Teilnehmer" Grid.Row="0" Foreground="DarkGray"/>

                                        <ToggleButton x:Name="btnTeilnehmerPinIt" Margin="0,0,5,0"
                                   HorizontalAlignment="Right" Width="25" 
                                   Height="25" Checked="HandleListePinning"
                                   Unchecked="HandleListeUnpinning" IsChecked="False">

                                            <Image x:Name="pinTeilnehmerImage" Source="Images\icons8-unpin-2-48.png"/>
                                        </ToggleButton>
                                    </Grid>

                                    <DataGrid  x:Name="teilnehmerDataGrid"  IsSynchronizedWithCurrentItem="True" ItemsSource="{Binding}"
                                   SelectionMode="Extended"     AutoGenerateColumns="False" Margin="2" IsReadOnly="True" 
                                   HeadersVisibility="Column"   Style="{StaticResource friendDataGridStyle}"
                                   RowStyle="{StaticResource friendDataGridRowStyle}"  >

                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Vorname" Binding="{Binding Vorname}"/>
                                            <DataGridTextColumn Header="Nachname"  Binding="{Binding Name}"/>
                                            <DataGridTextColumn Header="Persönliches Level" Binding="{Binding PersoenlichesLevel.Benennung}"/>
                                            <DataGridTextColumn Header="Skikurs" Width="*" Binding="{Binding Skikurs}"/>
                                        </DataGrid.Columns>

                                    </DataGrid>

                                </DockPanel>
                            </Border>
                            <GridSplitter Grid.Column="1"  HorizontalAlignment="Left" Width="10" Background="Transparent"/>
                        </Grid>


                    </Grid>

                </DockPanel>
            </TabItem>

            <TabItem Header="Skilehrer" x:Name="tabitemUebungsleiter" GotFocus="tabitemSkilehrer_GotFocus" Margin="-2,-2,0,-2" VerticalAlignment="Top">
                <TabItem.LayoutTransform>
                    <RotateTransform Angle="270"/>
                </TabItem.LayoutTransform>

                <DockPanel>
                    <!-- Dock="Right" - Button, um den "Skikursgruppen Explorer" aufzuklappen -->
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Panel.ZIndex="1">
                        <StackPanel.LayoutTransform>
                            <RotateTransform Angle="90"/>
                        </StackPanel.LayoutTransform>
                        <Button Margin="10,0,0,0" x:Name="btnShowSkilehrerExplorer" MouseEnter="HandleButtonExpMouseEnter" Content="Liste"/>
                        <DockPanel LastChildFill="False" Width="100"/>
                    </StackPanel>

                    <Grid  Grid.IsSharedSizeScope="True" >
                        <Grid x:Name="layerSkilehrerdetails" MouseEnter="HandleLayerdetailsMouseEnter" Background="White" Margin="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Grid x:Name="gridSkilehrerdetails" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>

                                <!-- Detailangaben -->
                                <Grid Grid.Row="1" Margin="2" >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="250"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label Grid.Column="0" Grid.Row="0" Content="Angezeigter Name" Margin="2"/>
                                    <Label Grid.Column="0" Grid.Row="1" Content="Vorname" Margin="2"/>
                                    <Label Grid.Column="0" Grid.Row="2" Content="Name" Margin="2"/>
                                    <TextBox Text="{Binding AngezeigterName, UpdateSourceTrigger=PropertyChanged}" 
                                        Grid.Column="1" Grid.Row="0" Margin="2"/>
                                    <TextBox Text="{Binding Vorname, UpdateSourceTrigger=PropertyChanged}"
                                        Grid.Column="1" Grid.Row="1"  Margin="2"/>
                                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                        Grid.Column="1" Grid.Row="2"  Margin="2"/>

                                    <Border Grid.Column="1" Grid.Row="3" BorderBrush="Black" BorderThickness="1" Margin="2" 
                                            AllowDrop="True"   DragOver="HandleDragOver" Drop="HandleDrop">
                                        <Grid>
                                            <Rectangle Fill="{StaticResource DefaultImageBrush}"/>
                                            <Image VerticalAlignment="Center" Source="/Images/icons8-ski-goggles-96.png" 
                                                   Visibility="{Binding Path=HatFoto,Converter={StaticResource BooleanToVisibilityInverted}}"/>
                                            <TextBlock TextAlignment="Center" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"  
                                                       Visibility="{Binding Path=HatFoto,Converter={StaticResource BooleanToVisibilityInverted}}">
                                                Neues Bild hierhin ziehen
                                            </TextBlock>
                                            <Image Source="{Binding Foto, UpdateSourceTrigger=PropertyChanged}"
                                                   Margin="2" Width="200" Height="260"/>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Grid>

                        </Grid>

                        <Grid x:Name="layerSkilehrerliste" Visibility="Visible" Margin="2" >
                            <!-- Grid, das den Skikursgruppen Explorer anzeigt-->
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition SharedSizeGroup="pinUebungsleiterSpalte" Width="280"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RenderTransform>
                                <TranslateTransform x:Name="layerSkilehrerlisteTrans"/>
                            </Grid.RenderTransform>

                            <Border x:Name="borderSkilehrerliste" BorderThickness="1" BorderBrush="Black" Grid.Column="1">
                                <DockPanel>


                                    <Grid x:Name="skilehrerGrid"  DockPanel.Dock="Top">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="Skilehrer" Grid.Row="0" Foreground="DarkGray"/>
                                        <ToggleButton x:Name="btnSkilehrerPinIt" Margin="0,0,5,0"
                                            HorizontalAlignment="Right" Width="25" 
                                            Height="25" Checked="HandleListePinning"
                                            Unchecked="HandleListeUnpinning" IsChecked="False">

                                            <Image x:Name="pinSkilehrerImage" Source="/Images/icons8-unpin-2-48.png"/>
                                        </ToggleButton>

                                    </Grid>
                                    <DataGrid x:Name="uebungsleiterDataGrid" Grid.Row="1" ItemsSource="{Binding}"
                                        SelectionMode="Extended"     AutoGenerateColumns="False" Margin="2" IsReadOnly="True" 
                                        HeadersVisibility="Column"   Style="{StaticResource friendDataGridStyle}"
                                        RowStyle="{StaticResource friendDataGridRowStyle}"   >
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Angezeigter Name" Binding="{Binding AngezeigterName}"/>
                                            <DataGridTextColumn Header="Vorname"  Binding="{Binding Vorname}"/>
                                            <DataGridTextColumn Header="Name" Width="*" Binding="{Binding Name}" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </DockPanel>
                            </Border>
                            <GridSplitter Grid.Column="1"  HorizontalAlignment="Left" Width="10" Background="Transparent"/>

                        </Grid>
                    </Grid>

                </DockPanel>
            </TabItem>

            <TabItem Header="Skikurse" x:Name="tabitemSkikurse" GotFocus="tabitemSkikurs_GotFocus" Margin="-2,-2,0,-2">
                <TabItem.LayoutTransform>
                    <RotateTransform Angle="270"/>
                </TabItem.LayoutTransform>

                <DockPanel>
                    <!-- Dock="Right" - Button, um den "Skikursgruppen Explorer" aufzuklappen -->
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Panel.ZIndex="1">
                        <Button Margin="10,0,0,0" x:Name="btnShowSkikursExplorer" MouseEnter="HandleButtonExpMouseEnter">Liste</Button>
                        <DockPanel LastChildFill="False" Width="100"/>
                        <StackPanel.LayoutTransform>
                            <RotateTransform Angle="90"/>
                        </StackPanel.LayoutTransform>
                    </StackPanel>

                    <Grid  IsSharedSizeScope="True" >
                        <Grid x:Name="layerSkikursdetails" MouseEnter="HandleLayerdetailsMouseEnter" 
                              Background="White" Margin="2">
                            
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Grid x:Name="gridSkikursdetails" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>

                                <!-- Detailangaben -->
                                <Grid Grid.Row="1" Margin="2" >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="250"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label Grid.Column="0" Grid.Row="0" Content="Gruppenname" Margin="2"/>
                                    <Label Grid.Column="0" Grid.Row="1" Content="Angezeigter Name" Margin="2"/>
                                    <Label Grid.Column="0" Grid.Row="2" Content="Gruppenlevel" Margin="2"/>
                                    <Label Grid.Column="0" Grid.Row="3" Content="Skilehrer" Margin="2"/>
                                    <Label Grid.Column="0" Grid.Row="4" Content="Anzahl Miglieder" Margin="2"/>
                                    <Label Grid.Column="0" Grid.Row="5" Content="Auswahl SL" Margin="2"/>
                                    <Label Grid.Column="0" Grid.Row="6" Content="Migliederliste" Margin="2"/>
                                    <TextBox Text="{Binding Path=Gruppenname, UpdateSourceTrigger=PropertyChanged}" 
                                        Grid.Column="1" Grid.Row="0" Margin="2"/>
                                    <TextBox Text="{Binding Path=AngezeigterGruppenname, UpdateSourceTrigger=PropertyChanged}"
                                             Grid.Column="1" Grid.Row="1"  Margin="2"/>
                                    <TextBox Text="{Binding Path=Gruppenlevel.Benennung, UpdateSourceTrigger=PropertyChanged}"
                                             Grid.Column="1" Grid.Row="2"  Margin="2"/>
                                    <TextBox Text="{Binding Path=Skilehrer.AngezeigterName, UpdateSourceTrigger=PropertyChanged}"
                                             Grid.Column="1" Grid.Row="3"  Margin="2"/>
                                    <TextBox Text="{Binding Path=AnzahlMitglieder,Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                             IsReadOnly="True" Grid.Column="1" Grid.Row="4"  Margin="2"/>
                                    <ComboBox x:Name="cbo" IsEditable="True"  IsReadOnly="True"
                                              DisplayMemberPath="AngezeigterName"
                                              SelectedItem="{Binding Skilehrer}" SelectedValue="AngezeigterName"
                                              Grid.Column="1" Grid.Row="5"  Margin="2"/>
                                    <DataGrid x:Name="skikursgruppenmitgliederDataGrid" Grid.Row="6" Grid.Column="1" 
                                              ItemsSource="{Binding Path=Mitgliederliste}" AutoGenerateColumns="False"
                                              IsReadOnly="True">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Vorname" Binding="{Binding Vorname}"/>
                                            <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </Grid>

                        </Grid>

                        <Grid x:Name="layerSkikursliste" Visibility="Visible" Margin="2" >
                            <!-- Grid, das den Skikursgruppen Explorer anzeigt-->
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition SharedSizeGroup="pinSkikursgruppeSpalte" Width="280"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RenderTransform>
                                <TranslateTransform x:Name="layerSkikurslisteTrans"/>
                            </Grid.RenderTransform>

                            <Border x:Name="borderSkigruppenliste" BorderThickness="1" BorderBrush="Black" Grid.Column="1">
                                <DockPanel>


                                    <Grid x:Name="skikursgruppenGrid"  DockPanel.Dock="Top">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="Skikursgruppen" Grid.Row="0" Foreground="DarkGray"/>
                                        <ToggleButton x:Name="btnSkikursPinIt" Margin="0,0,5,0"
                                            HorizontalAlignment="Right" Width="25" 
                                            Height="25" Checked="HandleListePinning"
                                            Unchecked="HandleListeUnpinning" IsChecked="False">

                                            <Image x:Name="pinSkikursImage" Source="Images\icons8-unpin-2-48.png"/>
                                        </ToggleButton>

                                    </Grid>
                                    <DataGrid x:Name="skikurseDataGrid" Grid.Row="1" ItemsSource="{Binding}"
                                              SelectionMode="Extended"     AutoGenerateColumns="False" Margin="2" IsReadOnly="True" 
                                              HeadersVisibility="Column"   Style="{StaticResource friendDataGridStyle}"
                                              RowStyle="{StaticResource friendDataGridRowStyle}"   >
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Gruppenname" Binding="{Binding Gruppenname}"/>
                                            <DataGridTextColumn Header="Angezeigter Name"  Binding="{Binding AngezeigterGruppenname}"/>
                                            <DataGridTextColumn Header="Skilehrer" Binding="{Binding Skilehrer.VollerName, Mode=OneWay}" />
                                            <DataGridTextColumn Header="Mitglieder" Width="*" Binding="{Binding AnzahlMitglieder,Mode=OneWay}" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </DockPanel>
                            </Border>
                            <GridSplitter Grid.Column="1"  HorizontalAlignment="Left" Width="10" Background="Transparent"/>

                        </Grid>
                    </Grid>

                </DockPanel>
            </TabItem>

            <TabItem Header="Levels" x:Name="tabitemLevels" GotFocus="tabitemLevel_GotFocus" Margin="-2,-2,0,-2">
                <TabItem.LayoutTransform>
                    <RotateTransform Angle="270"/>
                </TabItem.LayoutTransform>

                <DockPanel>
                    <!-- Dock="Right" - Button, um den "Skikursgruppen Explorer" aufzuklappen -->
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Panel.ZIndex="1">
                        <StackPanel.LayoutTransform>
                            <RotateTransform Angle="90"/>
                        </StackPanel.LayoutTransform>
                        <Button Margin="10,0,0,0" x:Name="btnShowLevelExplorer" MouseEnter="HandleButtonExpMouseEnter" Content="Liste"/>
                        <DockPanel LastChildFill="False" Width="100"/>
                    </StackPanel>

                    <Grid  Grid.IsSharedSizeScope="True" >
                        <Grid x:Name="layerLeveldetails" MouseEnter="HandleLayerdetailsMouseEnter" Background="White" Margin="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Grid x:Name="gridLeveldetails" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>

                                <!-- Detailangaben -->
                                <Grid Grid.Row="1" Margin="2" >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="250"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label Grid.Column="0" Grid.Row="0" Content="Benennung" Margin="2"/>
                                    <Label Grid.Column="0" Grid.Row="1" Content="Beschreibung" Margin="2"/>
                                    <TextBox Text="{Binding Benennung, UpdateSourceTrigger=PropertyChanged}" 
                                        Grid.Column="1" Grid.Row="0" Margin="2"/>
                                    <TextBox Text="{Binding Beschreibung, UpdateSourceTrigger=PropertyChanged}"
                                        Grid.Column="1" Grid.Row="1"  Margin="2" Height="Auto" TextWrapping="WrapWithOverflow"/>
                                </Grid>
                            </Grid>

                        </Grid>

                        <Grid x:Name="layerLevelliste" Visibility="Visible" Margin="2" >
                            <!-- Grid, das den Skikursgruppen Explorer anzeigt-->
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition SharedSizeGroup="pinLevelSpalte" Width="280"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RenderTransform>
                                <TranslateTransform x:Name="layerLevellisteTrans"/>
                            </Grid.RenderTransform>

                            <Border x:Name="borderLevelliste" BorderThickness="1" BorderBrush="Black" Grid.Column="1">
                                <DockPanel>


                                    <Grid x:Name="levelGrid"  DockPanel.Dock="Top">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="Levels" Grid.Row="0" Foreground="DarkGray"/>
                                        <ToggleButton x:Name="btnLevelPinIt" Margin="0,0,5,0"
                                            HorizontalAlignment="Right" Width="25" 
                                            Height="25" Checked="HandleListePinning"
                                            Unchecked="HandleListeUnpinning" IsChecked="False">

                                            <Image x:Name="pinLevelImage" Source="/Images/icons8-unpin-2-48.png"/>
                                        </ToggleButton>

                                    </Grid>
                                    <DataGrid x:Name="levelDataGrid" Grid.Row="1" ItemsSource="{Binding}"
                                        SelectionMode="Extended"     AutoGenerateColumns="False" Margin="2" IsReadOnly="True" 
                                        HeadersVisibility="Column"   Style="{StaticResource friendDataGridStyle}"
                                        RowStyle="{StaticResource friendDataGridRowStyle}"   >
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Benennung" Binding="{Binding Benennung}"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </DockPanel>
                            </Border>
                            <GridSplitter Grid.Column="1"  HorizontalAlignment="Left" Width="10" Background="Transparent"/>

                        </Grid>
                    </Grid>

                </DockPanel>
            </TabItem>

        </TabControl>




    </DockPanel>

</Window>

<!--
    Für Ribbon Lösung:
    Ribbon Workshop: https://www.rholtz-office.de/ribbonx/start
    -->
<!--<ribbon:Ribbon Name="MainRibbon" DockPanel.Dock="Top" ShowQuickAccessToolBarOnTop="False">
            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu Name="AppMenue"  
                                       SmallImageSource="/Images/icons8-menu-48.png">
                    <RibbonApplicationMenu.FooterPaneContent>
                        <DockPanel LastChildFill="False">
                            <RibbonButton Label="Beenden" Command="ApplicationCommands.Close"
                                DockPanel.Dock="Right"    ToolTip="Anwendung beenden (ALT+F4)"/>
                        </DockPanel>
                    </RibbonApplicationMenu.FooterPaneContent>
                    <RibbonApplicationMenuItem Header="_Neue Skireise" Command="ApplicationCommands.New" ImageSource="Images/neueGruppe-48.png"/>
                    <RibbonApplicationMenuItem Header="Skireise ö_ffnen" Command="ApplicationCommands.Open" ImageSource="Images/GruppeOeffnen-48.png"/>
                    <Separator/>
                    <RibbonApplicationMenuItem Header="Skireise _speichern" Command="ApplicationCommands.Save" ImageSource="Images/speichern-48.png"/>
                    <RibbonApplicationMenuItem Header="Skireise speichern _unter..." Command="ApplicationCommands.SaveAs"/>
                    <Separator/>
                    <RibbonApplicationMenuItem Header="Einteilungen _drucken..." Command="ApplicationCommands.Print" ImageSource="Images/drucken-48.png"/>
                    <Separator/>
                    <RibbonApplicationMenuItem Header="Zuletzt verwendet" x:Name="mostrecentlyUsedRibbonMenuItem"/>
                </RibbonApplicationMenu>
            </Ribbon.ApplicationMenu>
            <ribbon:RibbonTab Header="Datei"/>
        </ribbon:Ribbon>-->