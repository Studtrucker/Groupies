<Window x:Class="MainWindow1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Groupies"
        xmlns:uc="clr-namespace:Groupies.UserControls" 
        xmlns:cmd="clr-namespace:Groupies.Commands"
        xmlns:converters="clr-namespace:Groupies.Converters"
        xmlns:ViewModels="clr-namespace:Groupies.ViewModels"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        WindowState="Maximized" Icon="{Binding WindowTitleIcon}" Title="{Binding WindowTitleText}" Height="450" Width="800">

    <Window.Resources>
        <converters:StringToLeistungsstufeConverter x:Key="StringToLeistungsstufeConverter"/>
    </Window.Resources>
    
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding WindowLoadedCommand}"/>
        </i:EventTrigger>
        <i:EventTrigger EventName="Closed">
            <i:InvokeCommandAction Command="{Binding WindowClosedCommand}"/>
        </i:EventTrigger>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding WindowClosingCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    
    <DockPanel>
        <Menu DockPanel.Dock="Top" BorderThickness="1" BorderBrush="LightSlateGray">

            <MenuItem  Header="_Datei">
                <MenuItem Header="_Neuer Club" Command="{Binding ApplicationNewCommand}">
                    <MenuItem.Icon>
                        <Image Source="Images/neueGruppe-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Club ö_ffnen" Command="{Binding ApplicationOpenCommand}">
                    <MenuItem.Icon>
                        <Image Source="Images/GruppeOeffnen-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="Club _speichern" Command="{Binding ApplicationSaveCommand}">
                    <MenuItem.Icon>
                        <Image Source="Images/speichern-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Club speichern _unter..." Command="{Binding ApplicationSaveAsCommand}"/>
                <Separator/>
                <MenuItem  Header="Teilnehmerinfo _drucken..." Command="{Binding ApplicationPrintCommand}" CommandParameter="ParticipantInfo">
                    <MenuItem.Icon>
                        <Image Height="20" Source="Images/drucken-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Trainerinfo _drucken..." Command="{Binding ApplicationPrintCommand}"  CommandParameter="InstructorInfo"/>
                <Separator/>
                <MenuItem Header="Zuletzt verwendet" x:Name="mostrecentlyUsedMenuItem" />
                <Separator/>
                <MenuItem Header="_Beenden" Command="{Binding ApplicationCloseCommand}" />
            </MenuItem>


            <MenuItem Header="_Organisation">
                <MenuItem Header="Einteilungen">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/Images/icons8-diversity-48.png"/>
                    </MenuItem.Icon>
                    <MenuItem Header="Übersicht"  Command="{Binding EinteilungsuebersichtAnzeigenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-uebersichtsliste-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Erstellen" Command="{Binding EinteilungErstellenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-create-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="Gruppen">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/Images/icons8-konferenz-vordergrund-ausgewaehlte-48.png"/>
                    </MenuItem.Icon>
                    <MenuItem Header="Übersicht"  Command="{Binding GruppenuebersichtAnzeigenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-uebersichtsliste-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Erstellen"  Command="{Binding GruppeErstellenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-create-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="Leistungsstufen">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/Images/icons8-treppe-rauf-48.png"/>
                    </MenuItem.Icon>
                    <MenuItem Header="Übersicht"  Command="{Binding LeistungsstufenuebersichtAnzeigenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-uebersichtsliste-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Erstellen" Command="{Binding LeistungsstufeErstellenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-create-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="Fähigkeiten">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/Images/icons8-trophaee-48.png"/>
                    </MenuItem.Icon>
                    <MenuItem Header="Übersicht"  Command="{Binding FaehigkeitenuebersichtAnzeigenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-uebersichtsliste-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Erstellen" Command="{Binding FaehigkeitErstellenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-create-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    
                </MenuItem>
            </MenuItem>

            <MenuItem Header="_Personen" >
                <MenuItem Header="Teilnehmer">
                    <MenuItem Header="Übersicht"  Command="{Binding TeilnehmeruebersichtAnzeigenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-uebersichtsliste-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Erstellen" Command="{Binding TeilnehmerErstellenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-create-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem.Icon>
                        <Image Height="20" Source="Images/icons8-person-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Header="Trainer">
                    <MenuItem Header="Übersicht"  Command="{Binding TraineruebersichtAnzeigenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-uebersichtsliste-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Erstellen" Command="{Binding TrainerErstellenCommand}">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/icons8-create-48.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem.Icon>
                        <Image Height="20" Source="Images/icons8-trainer-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>

            <MenuItem  Header="_Extras" >
                <MenuItem Header="Daten aus Excel importieren">
                    <MenuItem.Icon>
                        <Image Height="20" Source="Images/icons8-person-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Daten nach Excel exportieren">
                    <MenuItem.Icon>
                        <Image Height="20" Source="Images/icons8-microsoft-excel-2019-48.png"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Hilfe">
                <MenuItem Header="Alle von Icons8" Command="{Binding OpenHomepageCommand}" >
                    <!--Für das Viewitem siehe WhatsApp ChatGPT vom 15.05.2025-->

                </MenuItem>
            </MenuItem>

        </Menu>


        <Grid x:Name="Desktop">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
                <ColumnDefinition Width="15*" SharedSizeGroup="GroupExplorerColumn" />
                <ColumnDefinition Width="Auto" SharedSizeGroup="GridSplitterColumn" />
                <ColumnDefinition Width="5*"  SharedSizeGroup="GroupViewerColumn" />
                <ColumnDefinition Width="13*"/>
                <ColumnDefinition Width="27*"/>
                <ColumnDefinition Width="Auto" SharedSizeGroup="GridSplitterColumn" />
                <ColumnDefinition Width="15*" SharedSizeGroup="PersonExplorerColumn" />
                <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Style="{StaticResource RandRowDefinition}"/>
                <RowDefinition />
                <RowDefinition Style="{StaticResource RandRowDefinition}"/>
            </Grid.RowDefinitions>

            <Grid x:Name="GridSpalte1" Grid.Column="1" Grid.Row="1" Margin="0,0,0,20" Grid.RowSpan="2" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="3*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0">
                    <TextBlock Text="Die Einteilungen" DockPanel.Dock="Top"/>

                    <DataGrid x:Name="EinteilungenDataGrid" DockPanel.Dock="Bottom"
                                  AutoGenerateColumns="False" 
                                  ItemsSource="{Binding AlleEinteilungen}"
                                  SelectedItem="{Binding SelectedEinteilung}"
                                  DisplayMemberPath="Benennung"
                                  IsReadOnly="True" CanUserAddRows="False" >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Sortierung"  Binding="{Binding Sortierung}" Width="Auto" />
                            <DataGridTextColumn Header="Benennung"  Binding="{Binding Benennung}" Width="*" />
                        </DataGrid.Columns>
      

                    </DataGrid>
                </DockPanel>

                <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch"  Height="5" 
                              VerticalAlignment="Center"  Background="WhiteSmoke"/>

                <DockPanel Grid.Row="2">
                    <TextBlock Text="Die Gruppen" DockPanel.Dock="Top"/>

                    <DataGrid x:Name="GruppenlisteDataGrid" 
                              ItemsSource="{Binding  SelectedEinteilung.EinteilungAlleGruppen}"  
                              SelectedItem="{Binding SelectedGruppe}"                             
                              DisplayMemberPath="Benennung" 
                              IsSynchronizedWithCurrentItem="True"
                              IsReadOnly="True" DockPanel.Dock="Bottom"
                              Margin="6"  Foreground="Black" 
                              AutoGenerateColumns="False" SelectionMode="Single">

                        <DataGrid.Resources>
                            <DataTemplate x:Key="GruppenlisteDataTemplate1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Column="0" Grid.Row="0" Text="Sortierung"/>
                                    <TextBlock Grid.Column="0" Grid.Row="2" Text="Interne Benennung"/>
                                    <TextBlock Grid.Column="0" Grid.Row="3" Text="Leistungsstufe"/>
                                    <TextBlock Grid.Column="0" Grid.Row="5" Text="Anzahl Mitglieder"/>
                                    <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Sortierung, UpdateSourceTrigger=PropertyChanged}"/>
                                    <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding Benennung,UpdateSourceTrigger=PropertyChanged}"/>
                                    <TextBlock Grid.Column="1" Grid.Row="3" Text="{Binding Leistungsstufe, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource StringToLeistungsstufeConverter}}"/>
                                    <TextBlock Grid.Column="1" Grid.Row="5" Text="{Binding Mitgliederliste.Count,UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>
                            </DataTemplate>

                        </DataGrid.Resources>

                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Löschen" Command="{Binding GruppeLoeschenCommand}" CommandParameter="{Binding SelectedGruppe}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>

                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Gruppen" Width="*" CellTemplate="{StaticResource GruppenlisteDataTemplate1}"/>
                            <DataGridTextColumn Header="Sort"  Binding="{Binding Sortierung}" Width="Auto" />
                            <DataGridTextColumn Header="Benennung"  Binding="{Binding Benennung}" Width="Auto" />
                            <DataGridTextColumn Header="Anz Teilnehmer"  Binding="{Binding Mitgliederliste.Count}" SortMemberPath="Mitgliederliste.Count" Width="Auto" />
                            <DataGridTextColumn Header="Stufe"  Binding="{Binding Leistungsstufe}" SortMemberPath="Leistungsstufe.Benennung" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>

                </DockPanel>

            </Grid>

            <GridSplitter x:Name="GridSplitterSpalte2" Grid.Column="2" Grid.Row="1" HorizontalAlignment="Center" 
                          Width="5" Background="WhiteSmoke" Margin="0,0,0,20" Grid.RowSpan="2"/>

            <DockPanel x:Name="DockPanelSpalte3"  Grid.Column="3" Grid.Row="1" Grid.ColumnSpan="3" Margin="0,0,0,20" Grid.RowSpan="2">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Gruppe" />
                        <uc:GruppendetailUserControl x:Name="GruppeUserControl"/>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>

            <GridSplitter x:Name="GridSplitterSpalte4" Grid.Column="6" Grid.Row="1" HorizontalAlignment="Center" 
                          Width="5" Background="WhiteSmoke" Margin="0,0,0,20" Grid.RowSpan="2"/>

            <Grid x:Name="GridSpalte5" Grid.Column="7" Grid.Row="1" Margin="0,0,0,20" Grid.RowSpan="2" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="3*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0">
                    <TextBlock Text="Gruppenlose Skilehrer" DockPanel.Dock="Top"/>
                    <DataGrid x:Name="GruppenloseTrainerDataGrid" DockPanel.Dock="Top"
                              ItemsSource="{Binding SelectedEinteilung.GruppenloseTrainer}"
                              SelectedItem="{Binding SelectedEinteilung.SelectedGruppenloserTrainer}"
                              IsSynchronizedWithCurrentItem="True" AutoGenerateColumns="False"
                              IsReadOnly="True" CanUserAddRows="False" >
                        
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseDoubleClick">
                                <i:InvokeCommandAction Command="{Binding TrainerEinteilenCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        
                        <DataGrid.Columns>
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding Foto}" Height="35" Width="35"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Name" Binding="{Binding VorUndNachname}" Width="*"/>
                        </DataGrid.Columns>

                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Trainer einteilen" Command="{Binding TeilnehmerEinteilenCommand}"/>
                                <MenuItem Header="Trainer erstellen" Command="{Binding TeilnehmerEinteilenCommand}"/>
                                <MenuItem Header="Trainer löschen"   Command="{Binding TeilnehmerEinteilenCommand}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>

                </DockPanel>

                <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch"  Height="5" 
                              VerticalAlignment="Center"  Background="WhiteSmoke"/>

                <DockPanel Grid.Row="2">
                    <TextBlock Text="Gruppenlose Teilnehmer" DockPanel.Dock="Top"/>
                    <DataGrid x:Name="GruppenloseTeilnehmerDataGrid" DockPanel.Dock="Top" 
                              ItemsSource="{Binding SelectedEinteilung.GruppenloseTeilnehmer}" 
                              SelectedItem="{Binding SelectedEinteilung.SelectedGruppenloserTeilnehmer}"
                              IsSynchronizedWithCurrentItem="True" AutoGenerateColumns="False"
                              CanUserAddRows="False">

                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseDoubleClick">
                                <i:InvokeCommandAction Command="{Binding TeilnehmerEinteilenCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Vorname" Binding="{Binding Vorname,UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding Nachname,UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Alter" Binding="{Binding Alter, Mode=OneWay}"/>
                            <DataGridComboBoxColumn x:Name="TeilnehmerLeistungsstandComboBox" Header="Leistungsstand" Width="*" SortMemberPath="Leistungsstufe.Benennung"
                                                    SelectedValueBinding="{Binding Leistungsstand, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource StringToLeistungsstufeConverter}}"/>
                        </DataGrid.Columns>
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Teilnehmer einteilen"  Command="{Binding TeilnehmerEinteilenCommand}"/>
                                <MenuItem Header="Teilnehmer erstellen"  Command="{Binding TeilnehmerErstellenCommand}"/>
                                <MenuItem Header="Teilnehmer bearbeiten" Command="{Binding TeilnehmerBearbeitenCommand}"/>
                                <MenuItem Header="Teilnehmer löschen"    Command="{Binding TeilnehmerLoeschenCommand}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>

                </DockPanel>

            </Grid>

        </Grid>
    </DockPanel>

</Window>

