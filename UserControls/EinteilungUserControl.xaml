<UserControl x:Class="UserControls.EinteilungUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Groupies"
             xmlns:rules="clr-namespace:Groupies.ValidationRules"
             mc:Ignorable="d"
             xmlns:cmd="clr-namespace:Groupies.Commands"
             Height="Auto" Width="600">

    <UserControl.Resources>
        <Style TargetType="TextBox" x:Key="IntegerValidation">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=(Validation.HasError), RelativeSource={RelativeSource Self}}" Value="True">
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </DataTrigger>
            </Style.Triggers>
            <Setter Property="Margin" Value="4"/>
        </Style>
        <Style TargetType="Label" x:Key="PflichtangabeLabel" >
            <Setter Property="ToolTip" Value="Pflichtangabe"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="Black" BorderThickness="1"  Margin="20">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="10" Color="DarkGray"/>
        </Border.Effect>

        <Grid Background="white" Panel.ZIndex="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Style="{StaticResource RandRowDefinition}"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Style="{StaticResource RandRowDefinition}"/>
            </Grid.RowDefinitions>

            <Label Grid.Column="1" Grid.Row="1" Style="{StaticResource PflichtangabeLabel}">
                <TextBlock>Sortierung <Run Foreground="Red" >*</Run></TextBlock>
            </Label>
            <Label Grid.Column="1" Grid.Row="2" Style="{StaticResource PflichtangabeLabel}">
                <TextBlock>Benennung <Run Foreground="Red" >*</Run></TextBlock>
            </Label>
            <Label Grid.Column="1" Grid.Row="3" Content="Beschreibung"/>


            <TextBox x:Name="SortierungTextBox" Grid.Column="2" Grid.Row="1" Width="50" HorizontalAlignment="Left">
                <Binding Path="Sortierung" UpdateSourceTrigger="Explicit"/>
            </TextBox>

            <TextBox x:Name="BenennungTextBox" Text="{Binding Benennung, UpdateSourceTrigger=Explicit}" 
                     Grid.Column="2" Grid.Row="2" />
            <TextBox Text="{Binding Beschreibung, UpdateSourceTrigger=Explicit}" 
                     Grid.Column="2" Grid.Row="3" 
                     TextWrapping="Wrap" AcceptsReturn="True" />
            
            <DockPanel Grid.Column="2" Grid.Row="4">
            </DockPanel>
            
        </Grid>
    </Border>
</UserControl>
    