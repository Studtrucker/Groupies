<UserControl x:Class="UserControls.FaehigkeitendetailUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Groupies"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <DockPanel>
        <!--Header-->
        <DockPanel DockPanel.Dock="Top">
            <Image Source="/Images/icons8-trophäe-48.png" 
                   Style="{StaticResource WindowImageCaption}" />
            <TextBlock Height="Auto" Text="Fähigkeit"
                       Style="{StaticResource WindowCaption}" />
        </DockPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition  Style="{StaticResource RandColumnDefinition}"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Style="{StaticResource RandRowDefinition}"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Style="{StaticResource RandRowDefinition}"/>
            </Grid.RowDefinitions>
            
            <Label Grid.Column="1" Grid.Row="1"
                   Content="Sortierung" />
            
            <TextBox Grid.Column="2" Grid.Row="1" 
                     Text="{Binding Sortierung, UpdateSourceTrigger=PropertyChanged}" 
                     Width="50" HorizontalAlignment="Left"
                     x:Name="NamingField"/>
            
            <Label Grid.Column="1" Grid.Row="2"
                   Content="Fähigkeit"/>
            <TextBox Grid.Column="2" Grid.Row="2"
                     Text="{Binding Benennung, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
            
            <Label Grid.Column="1" Grid.Row="3"
                   Content="Beschreibung"/>
            <TextBox Grid.Column="2" Grid.Row="3" 
                     Text="{Binding Beschreibung, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" 
                     TextWrapping="Wrap"/>

        </Grid>
        
    </DockPanel>

</UserControl>
