<UserControl x:Class="GroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Skiclub"
             xmlns:Services="clr-namespace:Skiclub.CurrentDataService"
             mc:Ignorable="d" 
             Width="Auto" Height="Auto">

    
    <Grid x:Name="gridSkikursdetails" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Detailangaben -->
        <Grid Grid.Row="1" Margin="2" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Label Grid.Column="1" Grid.Row="1" Content="Print Benennung" Margin="2"/>
            <Label Grid.Column="1" Grid.Row="2" Content="Benennung" Margin="2"/>
            <Label Grid.Column="1" Grid.Row="3" Content="Level" Margin="2"/>
            <Label Grid.Column="1" Grid.Row="4" Content="Skilehrer" Margin="2"/>
            <Label Grid.Column="1" Grid.Row="5" Content="Anzahl Miglieder" Margin="2"/>
            <Label Grid.Column="1" Grid.Row="6" Content="Migliederliste" Margin="2"/>
            
            <TextBox Grid.Column="2" Grid.Row="1" Text="{Binding Path=GroupPrintNaming, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Grid.Column="2" Grid.Row="2" Text="{Binding Path=GroupNaming, UpdateSourceTrigger=PropertyChanged}"  />
            <!--<TextBlock Grid.Column="2" Grid.Row="3" Text="{Binding Path=GroupLevel.LevelNaming, UpdateSourceTrigger=PropertyChanged}"/>-->
            <StackPanel Grid.Column="2" Grid.Row="3" Orientation="Horizontal">
                <ComboBox Grid.Column="2" Grid.Row="3" 
                          SelectedValuePath="LevelNaming" DisplayMemberPath="LevelNaming" IsSynchronizedWithCurrentItem="True"
                          SelectedValue="{Binding Path=GroupLevel.LevelNaming, UpdateSourceTrigger=PropertyChanged}"
                          SelectedItem="{Binding Path=GroupLevel.LevelNaming, UpdateSourceTrigger=PropertyChanged}"
                          x:Name="GroupLevelCombobox" />
               <TextBlock Text="{Binding GroupLevel.LevelNaming}"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Grid.Row="4" Orientation="Horizontal">
                <ComboBox Grid.Column="2" Grid.Row="4" 
                          SelectedValuePath="InstructorID" DisplayMemberPath="InstructorFullName" IsSynchronizedWithCurrentItem="True"
                          SelectedValue="{Binding Path=GroupLeader.InstructorID, UpdateSourceTrigger=PropertyChanged}"
                          SelectedItem="{Binding Path=GroupLeader.InstructorID, UpdateSourceTrigger=PropertyChanged}"
                          x:Name="GroupLeaderCombobox" />
                <TextBlock Text="{Binding GroupLeader.InstructorFullName}"/>
            </StackPanel>

            <TextBlock Grid.Column="2" Grid.Row="5" Text="{Binding Path=GroupMembers.Count,Mode=OneWay}" />

            <DataGrid Grid.Column="2" Grid.Row="6" x:Name="GroupMembersDataGrid" 
                      IsSynchronizedWithCurrentItem="true" AutoGenerateColumns="False"
                      IsReadOnly="True" Style="{StaticResource friendDataGridStyle}" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding ParticipantFullName}"/>
                    <DataGridTextColumn Header="Level" Binding="{Binding ParticipantLevel.LevelNaming}"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel  Grid.Column="2" Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="+" Click="AddParticipant" Width="100"/>
                <Button Content="~" Click="SetView" Width="80"/>
                <Button Content="-" Click="RemoveParticipant" Width="100"/>
            </StackPanel>
            <Canvas x:Name="Canvas"  Grid.Column="1" Grid.Row="7" Height="50" Background="LightBlue" Visibility="Hidden">

            </Canvas>
        </Grid>
    </Grid>

</UserControl>
