<UserControl x:Class="UserControls.GruppenstammUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Groupies"
             mc:Ignorable="d" 
             xmlns:convert="clr-namespace:Groupies.Converters"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:cmd="clr-namespace:Groupies.Commands"
             Height="Auto" Width="Auto">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding UserControlLoaded}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    
    <UserControl.Resources>
        <convert:StringToLeistungsstufeConverter x:Key="StringToLeistungsstufeConverter"/>
    </UserControl.Resources>

        <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Style="{StaticResource RandRowDefinition}"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
            <RowDefinition Style="{StaticResource RandRowDefinition}"/>
        </Grid.RowDefinitions>

        <!--Labels-->
            <Label Grid.Column="1" Grid.Row="1" Style="{StaticResource PflichtangabeLabel}">
                <TextBlock >Gruppen ID
                    <Run Foreground="Red">*</Run>
                </TextBlock>
            </Label>
            
            <Label Grid.Column="1" Grid.Row="2" Style="{StaticResource PflichtangabeLabel}">
            <TextBlock >Ausgabe Teilnehmerinfo
                    <Run Foreground="Red">*</Run>
            </TextBlock>
        </Label>

        <Label Grid.Column="1" Grid.Row="3" Style="{StaticResource PflichtangabeLabel}">
            <TextBlock>Benennung
                    <Run Foreground="Red">*</Run>
            </TextBlock>
        </Label>

        <Label Grid.Column="1" Grid.Row="4" Style="{StaticResource PflichtangabeLabel}">
            <TextBlock >Sortierer
                    <Run Foreground="Red">*</Run>
            </TextBlock>
        </Label>

        <Label Grid.Column="1" Grid.Row="5" Style="{StaticResource PflichtangabeLabel}">
            <TextBlock >Leistungsstufe
                    <Run Foreground="Red">*</Run>
            </TextBlock>
        </Label>
        <Label Grid.Column="1" Grid.Row="6" Style="{StaticResource PflichtangabeLabel}">
            <TextBlock >Mitglieder
                    <!--<Run Foreground="Red">*</Run>-->
            </TextBlock>
        </Label>

        <!--Felder-->
        <TextBox Text="{Binding GruppenID, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                     x:Name="GruppenIDTextBox" Grid.Column="2" Grid.Row="1"/>
        <TextBox Text="{Binding AusgabeTeilnehmerinfo, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                     x:Name="AusgabenameTextBox" Grid.Column="2" Grid.Row="2"/>

        <TextBox Text="{Binding Benennung, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" 
                     x:Name="BenennungTextBox" Grid.Column="2" Grid.Row="3" />

        <TextBox Text="{Binding Sortierung, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                     x:Name="SortierungTextBox" Grid.Column="2" Grid.Row="4"/>

        <ComboBox Grid.Column="2" Grid.Row="5" IsSynchronizedWithCurrentItem="True"
                  x:Name="LeistungsstufeCombobox"
                  ItemsSource="{Binding LeistungsstufenListCollectionView}"
                  SelectedValue="{Binding Leistungsstufe, UpdateSourceTrigger=PropertyChanged,
                    ValidatesOnNotifyDataErrors=True, Converter={StaticResource StringToLeistungsstufeConverter}}"/>


        <!--<DataGrid Grid.Column="2" Grid.Row="6" d:ItemsSource="{Binding Mitgliederliste}">-->
            <DataGrid Grid.Column="2" Grid.Row="6" x:Name="MitgliederlisteDataGrid" 
                          IsSynchronizedWithCurrentItem="True" AutoGenerateColumns="False" 
                          IsReadOnly="False" CanUserAddRows="False" 
                          ItemsSource="{Binding Mitgliederliste}" 
                          SelectionMode="Extended" >
            <!--MouseDoubleClick="{Binding TeilnehmerAusGruppeEntfernen}"-->
            <DataGrid.Columns>
                <DataGridTextColumn Header="Vorname" Binding="{Binding Vorname,UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Nachname,UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Alter" Binding="{Binding Alter, Mode=OneWay}"/>
                <DataGridComboBoxColumn x:Name="TeilnehmerLeistungsstufeComboBox" Header="Leistungsstufe" Width="*" 
                                                SelectedValueBinding="{Binding Leistungsstufe, Converter={StaticResource StringToLeistungsstufeConverter}}"/>
            </DataGrid.Columns>

                
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="{x:Static cmd:SkiclubCommands.TeilnehmerBearbeiten}" />
                    <MenuItem Command="{x:Static cmd:SkiclubCommands.TeilnehmerAusGruppeEntfernen}" />
                </ContextMenu>
            </DataGrid.ContextMenu>
                
        </DataGrid>

    </Grid>

</UserControl>
