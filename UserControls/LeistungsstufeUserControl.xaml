<UserControl x:Class="UserControls.LeistungsstufeUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Groupies"
             xmlns:cmd="clr-namespace:Groupies.Commands"
             mc:Ignorable="d" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             Height="Auto" Width="Auto">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding UserControlLoaded}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Background="white" Panel.ZIndex="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Style="{StaticResource RandRowDefinition}"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
            <RowDefinition Style="{StaticResource RandRowDefinition}"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="1" Grid.Row="1" Style="{StaticResource PflichtangabeLabel}">
            <TextBlock>Sortierung <Run Foreground="Red" >*</Run></TextBlock>
        </Label>
        <Label Grid.Column="1" Grid.Row="2" Style="{StaticResource PflichtangabeLabel}">
            <TextBlock>Benennung <Run Foreground="Red" >*</Run></TextBlock>
        </Label>
        <Label Grid.Column="1" Grid.Row="3" Content="Beschreibung"/>
        <Label Grid.Column="1" Grid.Row="4" Style="{StaticResource PflichtangabeLabel}">
            <TextBlock>Fähigkeiten <Run Foreground="Red" >*</Run></TextBlock>
        </Label>

        <TextBox x:Name="SortierungTextBox" Grid.Column="2" Grid.Row="1" Width="50" HorizontalAlignment="Left">
            <Binding Path="Sortierung" UpdateSourceTrigger="PropertyChanged"/>
        </TextBox>

        <TextBox x:Name="BenennungTextBox" Text="{Binding Benennung, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" 
                     Grid.Column="2" Grid.Row="2" />
        <TextBox Text="{Binding Beschreibung, UpdateSourceTrigger=PropertyChanged}" 
                     Grid.Column="2" Grid.Row="3" 
                     TextWrapping="Wrap" AcceptsReturn="True" />

        <DockPanel Grid.Column="2" Grid.Row="4">
            <DataGrid  x:Name="FaehigkeitenDataGrid"
                           AutoGenerateColumns="False" ItemsSource="{Binding Faehigkeiten,UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                           CanUserAddRows="True" SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <!--MenuItem implementiert das Interface 'ICommandSource'-->
                        <MenuItem Command="{x:Static cmd:SkiclubCommands.FaehigkeitNeuErstellen}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nr" Binding="{Binding Sortierung,UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />
                    <DataGridTextColumn Header="Fähigkeit" Binding="{Binding Benennung,UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" >
                        <DataGridTextColumn.ElementStyle>
                            <Style>
                                <Setter Property="TextBlock.TextTrimming" Value="WordEllipsis"/>
                                <Setter Property="TextBlock.TextWrapping" Value="Wrap"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Beschreibung" Binding="{Binding Beschreibung,UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style>
                                <Setter Property="TextBlock.TextTrimming" Value="WordEllipsis"/>
                                <Setter Property="TextBlock.TextWrapping" Value="Wrap"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
