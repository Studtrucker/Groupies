<UserControl x:Class="UserControls.LevelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Groupies"
             mc:Ignorable="d"
             xmlns:cmd="clr-namespace:Groupies.Commands"
             Height="Auto" MinWidth="200">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Content="Sortierung" />
        <Label Grid.Column="0" Grid.Row="1" Content="Benennung" />
        <Label Grid.Column="0" Grid.Row="2" Content="Fähigkeiten"/>

        <TextBox Text="{Binding SortNumber, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Column="1" Grid.Row="0" Width="50" HorizontalAlignment="Left"/>
        <TextBox Text="{Binding LevelNaming, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Column="1" Grid.Row="1" />
        <DockPanel Grid.Column="1" Grid.Row="2">
            <DataGrid  x:Name="SkillsDataGrid"
                       DockPanel.Dock="Top"
                       AutoGenerateColumns="False" ItemsSource="{Binding LevelSkills}"
                       IsReadOnly="True" Style="{StaticResource groupiesDataGridStyle}" 
                       CanUserAddRows="True" SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <!--MenuItem implementiert das Interface 'ICommandSource'-->
                        <MenuItem Command="{x:Static cmd:SkiclubCommands.NeuerSkill}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Sort" Binding="{Binding SortNumber}" />
                    <DataGridTextColumn Header="Fähigkeit" Binding="{Binding SkillNaming}" >
                        <DataGridTextColumn.ElementStyle>
                            <Style>
                                <Setter Property="TextBlock.TextTrimming" Value="WordEllipsis"/>
                                <Setter Property="TextBlock.TextWrapping" Value="Wrap"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style>
                                <Setter Property="TextBlock.TextTrimming" Value="WordEllipsis"/>
                                <Setter Property="TextBlock.TextWrapping" Value="Wrap"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
        <Button Content="Neu" Command="{x:Static cmd:SkiclubCommands.NeuerSkill}"
                Grid.Column="1" Grid.Row="3"/>
    </Grid>
</UserControl>
