<UserControl x:Class="PrintableSkikursgruppe"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Skischule"
             mc:Ignorable="d" 
             d:DesignHeight="1090" d:DesignWidth="790">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="Skigruppenname" Grid.Column="0" Text="Gruppe Besondere Experten" 
                   Grid.ColumnSpan="2" Margin="5" 
                   FontSize="48" FontWeight="Bold" />
        <Image x:Name="Skilehrerfoto" Width="160" Height="200" Grid.Row="1" Margin="5" Source="/Images/icons8-ski-goggles-96.png" />

        <TextBlock x:Name="Skilehrername" Grid.Column="1" Grid.Row="1" Text="Studti" FontSize="36" Margin="5" />
        <TextBlock x:Name="Gruppenmitglieder" Grid.Column="1" Grid.Row="2" Text="Teilnehmer" FontSize="36" Margin="5" />
        <DataGrid Grid.Row="3" Grid.Column="1" HeadersVisibility="None">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Vorname" Binding="{Binding Vorname}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
