<Window x:Class="TeilnehmerSuchErgebnis"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Groupies"
        mc:Ignorable="d"
        Title="Gefundene Teilnehmer" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Style="{StaticResource RandColumnDefinition}"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition  Style="{StaticResource RandColumnDefinition}"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Style="{StaticResource RandRowDefinition}"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Style="{StaticResource RandRowDefinition}"/>
        </Grid.RowDefinitions>
        <!--<Label Content="Suche nach"/>
        <TextBox  Grid.Column="1"/>-->
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Content="Anzahl Teilnehmer" Grid.Row="0"/>
            <Label Content="Anzahl Trainer" Grid.Row="1"/>
            <Label Content="Gruppiert nach Leistungsstufen" Grid.Row="2"/>
            <Label Content="Gruppiert nach Alter" Grid.Row="3"/>
            <TextBlock x:Name="TeilnehmerAnzahlTextBlock"  Grid.Row="0" Grid.Column="1"/>
            <TextBlock x:Name="TrainerAnzahlTextBlock"  Grid.Row="1" Grid.Column="1"/>
            <TextBlock x:Name="GruppierteLeistungsstufeTextBlock"  Grid.Row="2" Grid.Column="1"/>
            <TextBlock x:Name="GruppiertesAlterTextBlock" Grid.Row="3" Grid.Column="1"/>
        </Grid>
        <DataGrid x:Name="SuchergebnisDataGrid" ItemsSource="{Binding}" Grid.Row="2" Grid.Column="1" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Vorname" Binding="{Binding Teilnehmer.Vorname}" Width="Auto"/>
                <DataGridTextColumn Header="Nachname" Binding="{Binding Teilnehmer.Nachname}" Width="Auto"/>
                <DataGridTextColumn Header="Leistungsstand Teilnehmer" Binding="{Binding Teilnehmer.Leistungsstand.Benennung}" Width="Auto"/>
                <DataGridTextColumn Header="Sort Gruppe" Binding="{Binding Gruppe.Sortierung}" Width="Auto"/>
                <DataGridTextColumn Header="Interner Name" Binding="{Binding Gruppe.Benennung}" Width="Auto"/>
                <DataGridTextColumn Header="Ausgabename" Binding="{Binding Gruppe.AusgabeTeilnehmerinfo}" Width="Auto"/>
                <DataGridTextColumn Header="Leistungsstufe Gruppe" Binding="{Binding Gruppe.Leistungsstufe.Benennung}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
