<Window x:Class="TrainerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Groupies"
        xmlns:cmd="clr-namespace:Groupies.Commands"
        xmlns:convert="clr-namespace:Groupies.Converters"
        xmlns:rules="clr-namespace:Groupies.ValidationRules"
        mc:Ignorable="d"
        Loaded="HandleWindowLoaded"
        Title="Trainer" SizeToContent="Height" Width="700">

    <Window.BindingGroup>
        <BindingGroup >
            <BindingGroup.ValidationRules >
                <rules:TrainerValidationRule/>
            </BindingGroup.ValidationRules>
        </BindingGroup>
    </Window.BindingGroup>

    <Window.Resources>
        <convert:InvertableBoolToVisibleConverter Inverted="True" x:Key="BooleanToVisibilityInverted"/>
        <!--<Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="Background" Value="Coral"/>
                </Trigger>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>-->
    </Window.Resources>
    
    <DockPanel>
        <!--Header-->
        <DockPanel DockPanel.Dock="Top">
            <Image Source="/Images/icons8-trainer-48.png"
            Style="{StaticResource WindowImageCaption}" />

            <TextBlock Height="Auto" Text="Trainer" x:Name="Titel"
                       Style="{StaticResource WindowCaption}"/>
        </DockPanel>


        <!--OK- und Cancel-Button-->
        <Border DockPanel.Dock="Bottom">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Label x:Name="FehlerInfoLabel" Content="{Binding (Validation.ValidationAdornerSiteFor).(Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Mode=Self}}" />
                <Button x:Name="OkButton" Width="110" Content="Übernehmen" Click="HandleOkButton"/>
                <Button x:Name="CancelButton" Width="110" Content="Verwerfen" Click="HandleCancelButton"/>
                <Button x:Name="SchliessenButton" Width="110" Content="Schließen" Click="SchliessenButton_Click"/>
            </StackPanel>
        </Border>
        <!--Inputfields-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Style="{StaticResource RandColumnDefinition}"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Style="{StaticResource RandRowDefinition}"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Style="{StaticResource RandRowDefinition}"/>
            </Grid.RowDefinitions>

            <Border AllowDrop="True" Width="120" Height="180" Grid.Row="1" Grid.Column="1" Grid.RowSpan="4" 
                    VerticalAlignment="Top" Margin="2" DragOver="HandleDragOver" Drop="HandleDrop" 
                    BorderBrush="Black" BorderThickness="1">
                
                <Grid>
                    <Rectangle Fill="Transparent"/>
                    <Image VerticalAlignment="Center" Source="/Images/icons8-trainer-96.png" 
                           Visibility="{Binding Path=HasPicture,Converter={StaticResource BooleanToVisibilityInverted}}"/>
                    <TextBlock TextAlignment="Center" Foreground="red" HorizontalAlignment="Center" VerticalAlignment="Center"  
                               Visibility="{Binding Path=HasPicture,Converter={StaticResource BooleanToVisibilityInverted}}">Bild per
							<LineBreak/> Drag'n'Drop<LineBreak/>setzen
                    </TextBlock>
                    <Image x:Name="FotoImage" Width="96" Height="96" Source="{Binding Foto}" />
                </Grid>

            </Border>

            <Label Grid.Column="2" Grid.Row="1" Style="{StaticResource PflichtangabeLabel}">
                <TextBlock>Trainer ID
				<Run Foreground="Red">*</Run>
                </TextBlock>
            </Label>
            <TextBox x:Name="TrainerIDTextBox" Grid.Column="3" Grid.Row="1" IsReadOnly="True">
                <Binding Path="TrainerID" />
            </TextBox>
            
            <Label Grid.Column="2" Grid.Row="2" Style="{StaticResource PflichtangabeLabel}">
                <TextBlock>Spitzname
				<Run Foreground="Red">*</Run>
                </TextBlock>
            </Label>
            <TextBox x:Name="SpitznameTextBox" Grid.Column="3" Grid.Row="2">
                <Binding Path="Spitzname" UpdateSourceTrigger="Explicit" />
            </TextBox>
                
            <Label Grid.Column="2" Grid.Row="3" Style="{StaticResource PflichtangabeLabel}">
                <TextBlock>Vorname
                    <Run Foreground="Red">*</Run>
                </TextBlock>
            </Label>
            <TextBox Grid.Column="3" Grid.Row="3"
                     x:Name="VornameTextbox"
                     Text="{Binding Vorname, UpdateSourceTrigger=Explicit}"   />

            <Label Content="Nachname" Grid.Column="2" Grid.Row="4"/>
            <TextBox Grid.Column="3" Grid.Row="4"
                     x:Name="NachnameTextbox"
                     Text="{Binding Nachname, UpdateSourceTrigger=Explicit}" />

            <Label Content="Telefon" Grid.Column="2" Grid.Row="5"/>
            <TextBox Grid.Column="3" Grid.Row="5"
                     x:Name="TelefonTextbox"
                     Text="{Binding Telefonnummer, UpdateSourceTrigger=Explicit}" />
            
            <Label Content="e-Mail" Grid.Column="2" Grid.Row="6"/>
            <TextBox Grid.Column="3" Grid.Row="6"
                     x:Name="eMailTextbox"
                     Text="{Binding EMail, UpdateSourceTrigger=Explicit}" />
        </Grid>

    </DockPanel>
</Window>
